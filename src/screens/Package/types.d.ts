namespace PropTypes {
    type PackageBuyFeedback = {
        route: import('@react-navigation/native').RouteProp<>
    }
    type PackageSingle = {
        route: import('@react-navigation/native').RouteProp
    }
}