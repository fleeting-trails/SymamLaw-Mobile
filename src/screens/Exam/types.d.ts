namespace PropTypes {
    type AllExamsByCategory = {
        route: import('@react-navigation/native').RouteProp
    }
    type ExamStart = {
        route: import('@react-navigation/native').RouteProp
    }
}