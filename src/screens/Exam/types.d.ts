namespace PropTypes {
    type AllExamsByCategory = {
        route: import('@react-navigation/native').RouteProp
    }
    type AllExamsSubCategories = {
        route: import('@react-navigation/native').RouteProp
    }
    type ExamStart = {
        route: import('@react-navigation/native').RouteProp
    }
    type Exam = {
        route: import('@react-navigation/native').RouteProp
    }
    type ExamResult = {
        route: import('@react-navigation/native').RouteProp
    }
    type ExamDetails = {
        route: import('@react-navigation/native').RouteProp
    }
    type ExamPreview = {
        route: import('@react-navigation/native').RouteProp
    }
}
